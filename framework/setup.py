#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name="ossec-api",
    version="1.0.0",
    description="Control OSSEC with OSSEC API",
    package=find_packages(),
    install_requires=[],
)